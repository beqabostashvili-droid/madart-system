{
  "name": "@madart/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed REST + realtime client shared by every frontend. Shipped as TypeScript source (consumers transpile).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@madart/domain": "workspace:*",
    "@madart/types": "workspace:*",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@madart/config": "workspace:*",
    "eslint": "^9.36.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.44.0"
  }
}
